<div id="session">
  <%= current_user.full_name %> (<%= current_user.login %>)
  <%= link_to t('views.logout'), user_session_path, :method => :delete, :confirm => "Are you sure you want to logout?" %>
</div>

