
<%=
  present :elegant do |p|
    p.header do |r|
      r.field 'Title', @resource.title
      r.field 'Volumes', @resource.volumes
      r.field 'Published on', @resource.published_on
    end
    p.body do |r|
      r.field 'Authors', @resource.author_names
      r.field 'Abstract', @resource.abstract
      r.field 'Description', @resource.description
      r.field 'Tagged with', @resource.tag_names
      r.field 'Volumes', @resource.volumes
      r.field 'Published on', @resource.published_on
    end
    p.actions do |r|
#      r.text link_to('manage some nested resource', path_to_some_resource_path(@resource))
      r.text edit_action
      r.text destroy_action
      r.text back_action
    end
  end
%>

