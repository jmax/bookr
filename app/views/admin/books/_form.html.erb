<%= f.select :category_id, Category.to_select %>
<%= f.text_field :title %>
<%= f.text_area :abstract %>
<%= f.text_area :description %>
<%= f.text_field :volumes %>
<%= f.date_select :published_on %>

<%= f.has_and_belongs_to_many :authors, :full_name, Author.all %>
<div style="clear:both" />

<%= f.has_and_belongs_to_many :tags, :name, Tag.all(:order => :name) %>
<div style="clear:both" />

